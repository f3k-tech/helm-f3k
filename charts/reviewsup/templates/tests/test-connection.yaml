apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reviewsup.fullname" . }}-test-connection"
  labels:
    {{- include "reviewsup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reviewsup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
