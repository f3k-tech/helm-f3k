apiVersion: v1
kind: Service
metadata:
  name: {{ include "reviewsup.fullname" . }}
  labels:
    {{- include "reviewsup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reviewsup.selectorLabels" . | nindent 4 }}
