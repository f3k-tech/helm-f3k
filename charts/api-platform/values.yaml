replicaCount: 1

image:
  repository: registry.git.f3k.tech/f3k/repo/development
  pullPolicy: IfNotPresent
  tag: "latest"

imagePullSecrets:
  - name: gitlab-registry

# Random secrets will be automatically generated by the chart if not provided
secrets:
  appSecret: ""
  jwtPassphrase: ""

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}

securityContext: {}

service:
  type: ClusterIP
  port: 9000

ingress:
  enabled: false
  className: ""
  annotations:
    {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

nginx:
  enabled: true
  image: nginx:latest
  service:
    ports:
      http: 80
  config:
    client_max_body_size: 16G
    client_body_timeout: 3600s
    send_timeout: 3600s
    keepalive_timeout: 60s

php:
  config:
    max_execution_time: 3600
    max_input_time: 3600
    memory_limit: 2048M
    post_max_size: 16G
    upload_max_filesize: 16G
    max_file_uploads: 20
    default_socket_timeout: 3600

mysqlIsAlive:
  enabled: false
  image:
    repository: bitnami/mysql
    pullPolicy: IfNotPresent
    tag: "8.0.36"

mysql:
  enabled: false
  auth:
    rootPassword: ""
    username: "api-platform"
    password: ""
    database: api-platform
    # existingSecret: "api-platfom-mysql"
  primary:
    persistence:
      enabled: false
      # storageClass: "-"
      # existingClaim: api-platform-mysql
      accessMode: ReadWriteOnce
      size: 10Gi
      annotations: {}

mariadb:
  enabled: false
  auth:
    rootPassword: ""
    username: "api-platform"
    password: ""
    database: api-platform
    # existingSecret: "api-platfom-mysql"
  primary:
    persistence:
      enabled: false
      # storageClass: "-"
      # existingClaim: api-platform-mysql
      accessMode: ReadWriteOnce
      size: 10Gi
      annotations: {}

persistence:
  sqlite:
    enabled: false
    # existingClaim: api-platform-sqlite
    annotations: {}
    accessMode: ReadWriteOnce
    size: 1Gi
    storageClass: ""

resources: {}

livenessProbe:
  enabled: false
  httpGet:
    path: /
    port: http
readinessProbe:
  enabled: false
  httpGet:
    path: /
    port: http

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

volumes: []

volumeMounts: []

nodeSelector: {}

tolerations: []

affinity: {}
